{
  "coerce": true,
  "description": "Yandex Cloud service account authorized key (write-only).",
  "fields": [
    {
      "description": "Contents of the authorized-key file created with `yc iam key create --output key.json`.",
      "group": "Credentials",
      "name": "sa_key_json",
      "required": true,
      "rules": [
        {
          "expr": "this.contains(\"service_account_id\") \u0026\u0026 this.contains(\"private_key\") \u0026\u0026 this.contains(\"\\\"id\\\"\")",
          "id": "sa-key-shape",
          "message": "not a Yandex Cloud authorized key: id, service_account_id and private_key are required"
        }
      ],
      "secret": true,
      "string": {
        "maxLen": "65536",
        "minLen": "64"
      },
      "title": "Authorized key (JSON)"
    }
  ],
  "id": {
    "name": "yandex.credentials",
    "namespace": "provider",
    "version": "v1.0.0"
  },
  "strict": true
}

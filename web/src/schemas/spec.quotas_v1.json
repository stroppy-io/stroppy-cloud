{
  "id":  {
    "namespace":  "spec",
    "name":  "quotas",
    "version":  "v1.0.0"
  },
  "description":  "Params of stroppy-quotas: read the provider limits and usage of one profile.",
  "fields":  [
    {
      "name":  "provider",
      "description":  "Cloud the credentials belong to.",
      "required":  true,
      "choice":  {
        "options":  [
          {
            "value":  {
              "stringValue":  "yandex"
            },
            "label":  "Yandex Cloud"
          },
          {
            "value":  {
              "stringValue":  "aws"
            },
            "label":  "AWS"
          }
        ]
      },
      "group":  "Provider",
      "title":  "Provider"
    },
    {
      "name":  "settings",
      "description":  "Baked provider.<kind>.settings value; picks the folder or account to read.",
      "required":  true,
      "json":  {},
      "group":  "Provider",
      "title":  "Settings"
    },
    {
      "name":  "credentials_secret",
      "description":  "Name of the Graphene secret holding the credentials — never the value.",
      "required":  true,
      "string":  {
        "pattern":  "^[a-z0-9][a-z0-9._-]{0,62}$"
      },
      "group":  "Provider",
      "title":  "Credentials secret"
    },
    {
      "name":  "location",
      "description":  "Zone (yandex) or region (aws) to read zone-scoped quotas for; empty reads all.",
      "string":  {
        "maxLen":  "64"
      },
      "group":  "Provider",
      "title":  "Location"
    }
  ],
  "strict":  true,
  "coerce":  true
}

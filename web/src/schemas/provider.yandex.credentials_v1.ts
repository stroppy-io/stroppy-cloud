// GENERATED from schemapb schema provider.yandex.credentials@1 — do not edit.
// Yandex Cloud service account authorized key (write-only).

/** root */
export interface ProviderYandexCredentials1 {
  /** Authorized key (JSON). Contents of the authorized-key file created with `yc iam key create --output key.json`. */
  sa_key_json: string;
}

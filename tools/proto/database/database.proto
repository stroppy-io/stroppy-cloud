syntax = "proto3";

package database;

import "database/picodata.proto";
import "database/postgres.proto";
import "validate/validate.proto";

option go_package = "github.com/stroppy-io/hatchet-workflow/internal/proto/database";

message Database {
    message Template {
        oneof template {
            option (validate.required) = true;
            Postgres.Instance postgres_instance = 1;
            Postgres.Cluster postgres_cluster = 2;
            Picodata.Instance picodata_instance = 3;
            Picodata.Cluster picodata_cluster = 4;
        }
    }
    Template template = 1 [(validate.rules).message.required = true];
}

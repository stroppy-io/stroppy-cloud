{
  "coerce": true,
  "description": "Provider quota snapshot: limits and current usage, as observed at a point in time.",
  "fields": [
    {
      "description": "permission_denied when cloud quotas cannot be read. Quotas are empty and launches rely on provider enforcement instead of the cloud precheck.",
      "group": "Result",
      "name": "unavailable_reason",
      "string": {
        "maxLen": "64"
      },
      "title": "Unavailable reason"
    },
    {
      "description": "Scope of the quota snapshot or denied request, e.g. cloud:b1g... for Yandex Cloud. Cloud quotas cover all folders and zones.",
      "group": "Result",
      "name": "scope",
      "string": {
        "maxLen": "256"
      },
      "title": "Scope"
    },
    {
      "description": "When the provider was read; the server treats a snapshot older than a minute as stale.",
      "group": "Result",
      "name": "observed_at",
      "required": true,
      "timestamp": {},
      "title": "Observed at"
    },
    {
      "description": "One entry per quota the provider reports for this profile.",
      "group": "Result",
      "list": {
        "items": [
          {
            "object": {
              "schema": {
                "fields": [
                  {
                    "description": "Provider quota id, e.g. compute.instanceCores.count.",
                    "name": "name",
                    "required": true,
                    "string": {
                      "maxLen": "128",
                      "minLen": "1"
                    },
                    "title": "Quota"
                  },
                  {
                    "description": "Upper bound the provider enforces.",
                    "double": {
                      "gte": 0
                    },
                    "name": "limit",
                    "required": true,
                    "title": "Limit"
                  },
                  {
                    "description": "Consumption at observed_at across the quota scope: all folders in the cloud for Yandex Cloud, or the AWS account.",
                    "double": {
                      "gte": 0
                    },
                    "name": "used",
                    "required": true,
                    "title": "Used"
                  },
                  {
                    "description": "Unit of limit and used: count, GB, GiB…",
                    "name": "unit",
                    "string": {
                      "maxLen": "32"
                    },
                    "title": "Unit"
                  },
                  {
                    "description": "Set for a zone-scoped quota; empty when the quota is regional or global.",
                    "name": "zone",
                    "string": {
                      "maxLen": "64"
                    },
                    "title": "Zone"
                  }
                ],
                "strict": true
              }
            }
          }
        ],
        "maxItems": "512"
      },
      "name": "quotas",
      "required": true,
      "title": "Quotas"
    }
  ],
  "id": {
    "name": "result.quotas",
    "namespace": "spec",
    "version": "v1.0.0"
  },
  "strict": true
}

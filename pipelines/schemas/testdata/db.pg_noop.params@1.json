{
  "coerce": true,
  "description": "PostgreSQL wire-protocol blackhole; measures the delivery ceiling with no storage engine behind it.",
  "fields": [
    {
      "choice": {
        "default": {
          "stringValue": "0.1.2"
        },
        "options": [
          {
            "label": "0.1.2",
            "value": {
              "stringValue": "0.1.2"
            }
          }
        ]
      },
      "description": "Build of the pg-noop server; the binary is served through the stroppy gateway.",
      "group": "Engine",
      "name": "version",
      "required": true,
      "title": "pg-noop version"
    },
    {
      "description": "Connection-handling workers; 0 = one per CPU of the machine.",
      "group": "Server",
      "int64": {
        "default": "0",
        "gte": "0",
        "lte": "4096"
      },
      "name": "workers",
      "title": "Workers"
    },
    {
      "description": "Artificial delay added before each response, to model a storage engine that is not free.",
      "group": "Behavior",
      "int64": {
        "default": "0",
        "gte": "0",
        "lte": "60000"
      },
      "name": "latency_ms",
      "title": "Injected latency",
      "unit": "ms"
    },
    {
      "description": "Fraction of statements answered with an ErrorResponse instead of a result, to exercise the driver's error path. 0 = never, 1 = always.",
      "double": {
        "default": 0,
        "gte": 0,
        "lte": 1
      },
      "group": "Behavior",
      "name": "error_rate",
      "title": "Error rate"
    },
    {
      "description": "TCP port the blackhole listens on.",
      "group": "Server",
      "int64": {
        "default": "5432",
        "gte": "1",
        "lte": "65535"
      },
      "name": "port",
      "title": "Port"
    }
  ],
  "id": {
    "name": "pg_noop.params",
    "namespace": "db",
    "version": "v1.0.0"
  },
  "rules": [
    {
      "expr": "root.error_rate == 0.0 || root.latency_ms == 0",
      "id": "one-knob-at-a-time",
      "message": "injecting both latency and errors makes the ceiling unattributable",
      "severity": "SEVERITY_WARNING"
    }
  ],
  "strict": true
}
